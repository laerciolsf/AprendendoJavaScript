// Define a função soma que recebe dois parâmetros, n1 e n2, e retorna a soma desses parâmetros
function soma(n1, n2) {
    return n1 + n2;
}

// Chama a função soma com os argumentos 2 e 5, e imprime o resultado
console.log(soma(2, 5)); // Saída: 7
