// Define uma função anônima que recebe um parâmetro x e retorna x multiplicado por 2
let v = function(x) {
    return x * 2;
}

// Chama a função v com o argumento 5 e imprime o resultado
console.log(v(5)); // Saída: 10
